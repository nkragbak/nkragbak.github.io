document.querySelectorAll('[data-year]').forEach(el=>el.textContent=new Date().getFullYear());const menuButton=document.querySelector('.menu-toggle');const nav=document.querySelector('.site-nav');if(menuButton&&nav){menuButton.addEventListener('click',()=>{const open=nav.classList.toggle('open');menuButton.setAttribute('aria-expanded',String(open));});}const search=document.querySelector('#politics-search');const sections=[...document.querySelectorAll('.topic-section')];const noResults=document.querySelector('#no-results');if(search&&sections.length){search.addEventListener('input',()=>{const query=search.value.trim().toLocaleLowerCase('da');let matches=0;sections.forEach(section=>{const haystack=(section.dataset.search+' '+section.textContent).toLocaleLowerCase('da');const visible=!query||haystack.includes(query);section.hidden=!visible;if(visible)matches+=1;});if(noResults)noResults.hidden=matches!==0;});}